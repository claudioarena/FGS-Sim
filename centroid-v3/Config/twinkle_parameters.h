#ifndef TWINKLE_PAR
#define TWINKLE_PAR
#include "const_parameters.h"

#define CONFIG "Twinkle"
#define TEL_DIAM 450             //in mm
static const float emiss = 0.02; // Mirror emissivity
static const struct filter FGS_filter = B_filter;

#endif